import { writable } from 'svelte/store'

export const logo = writable(null)
export const logoAlt = writable(null)
export const links = writable(null)
export const members = writable([])
export const resources = writable([])